# HealSweets - オンラインスイーツストア

健康志向のスイーツを販売するECサイトのポートフォリオプロジェクトです。

## 技術スタック

- **Backend**: Java 21, Spring Boot 3.2
- **Frontend**: Thymeleaf, HTML5, CSS3, JavaScript
- **Database**: MySQL 8.0 / H2 (開発用)
- **Security**: Spring Security
- **ORM**: Spring Data JPA / Hibernate

## 機能一覧

### 顧客向け機能
- 🏠 **トップページ**: 商品一覧、カテゴリーフィルタリング、検索機能
- 📦 **商品詳細**: 商品情報、アレルギー情報表示
- 🛒 **ショッピングカート**: 商品追加・削除・数量変更
- 💳 **チェックアウト**: 配送先設定、支払い方法選択、注文確定
- 👤 **会員機能**: 登録・ログイン・マイページ
- 🥗 **アレルギー管理**: 会員のアレルギー情報登録
- 📧 **お問い合わせ**: フォームからの問い合わせ

### 管理者機能（新機能）
- 📊 **ダッシュボード**: 統計情報の一覧表示（会員数、商品数、売上など）
- 👥 **会員管理**: 会員一覧・無効化・復元
- 🛍️ **商品管理**: 商品一覧・追加・編集・無効化・有効化
- 📦 **注文管理**: 全注文の確認とステータス管理

### カテゴリー
- アレルゲンフリー
- 低糖質
- カカオ70%

## プロジェクト構造

```
healsweets/
├── src/main/java/com/healsweets/
│   ├── HealSweetsApplication.java    # メインクラス
│   ├── config/                       # 設定クラス
│   │   ├── SecurityConfig.java       # Spring Security設定
│   │   └── DataInitializer.java      # 初期データ投入
│   ├── controller/                   # コントローラー
│   │   ├── HomeController.java
│   │   ├── ProductController.java
│   │   ├── AuthController.java
│   │   ├── MemberController.java
│   │   ├── CartController.java
│   │   ├── CheckoutController.java
│   │   └── AdminController.java      # 管理者コントローラー（新規）
│   ├── entity/                       # エンティティ
│   │   ├── Member.java               # roleフィールド追加
│   │   ├── Product.java
│   │   ├── CartItem.java
│   │   ├── Order.java
│   │   ├── OrderItem.java
│   │   └── Contact.java
│   ├── repository/                   # リポジトリ
│   ├── service/                      # サービス
│   │   └── AdminService.java         # 管理者サービス（新規）
│   └── dto/                          # DTOクラス
│       └── ProductAdminDto.java      # 商品管理用DTO（新規）
├── src/main/resources/
│   ├── templates/                    # Thymeleafテンプレート
│   │   ├── index.html
│   │   ├── fragments/
│   │   ├── auth/
│   │   ├── product/
│   │   ├── member/
│   │   ├── cart/
│   │   ├── checkout/
│   │   └── admin/                    # 管理者ページ（新規）
│   │       ├── dashboard.html
│   │       ├── member-list.html
│   │       ├── product-list.html
│   │       ├── product-form.html
│   │       └── order-list.html
│   ├── static/
│   │   ├── css/
│   │   │   └── admin-style.css       # 管理者ページCSS（新規）
│   │   └── images/
│   ├── application.properties        # 設定ファイル
│   └── application-mysql.properties  # MySQL用設定
├── sql/
│   ├── init_database.sql
│   └── add_admin_role.sql            # 管理者機能追加SQL（新規）
└── pom.xml
```

## セットアップ手順

### 1. 前提条件
- Java 21 以上
- Maven 3.8 以上
- MySQL 8.0 以上（MySQL使用時）

### 2. データベース設定

#### 初期データベースの作成
```bash
# MySQLにログインして実行
mysql -u root -p < sql/init_database.sql
```

#### 管理者機能の追加（重要）
管理者機能を使用するには、以下のSQLを実行してデータベースを更新してください：

```bash
mysql -u root -p < sql/add_admin_role.sql
```

このSQLは以下を実行します：
- Memberテーブルにroleカラムを追加
- 既存の会員（ID=1）を管理者に設定
- 管理者用テストアカウントを追加

### 3. アプリケーション起動

#### MySQLで起動
```bash
mvn spring-boot:run -Dspring-boot.run.profiles=mysql
```

#### H2データベースで起動（開発用）
```bash
mvn spring-boot:run
```

### 4. アクセス
- アプリケーション: http://localhost:8080
- 管理者ダッシュボード: http://localhost:8080/admin
- H2コンソール（開発用）: http://localhost:8080/h2-console

### 5. テストアカウント

#### 一般会員
- Email: `tanaka@example.com`
- Password: `password123`

#### 管理者（add_admin_role.sql実行後）
- Email: `admin@healsweets.com`
- Password: `admin123`

## 管理者機能の使い方

### 1. 管理者としてログイン
- 管理者アカウントでログイン後、`/admin` にアクセスします
- 一般ユーザーは管理者ページにアクセスできません

### 2. ダッシュボード
- 会員数、商品数、注文数、売上などの統計情報を表示
- クイックアクセスで各管理機能にアクセス
- 最近の注文を一覧表示

### 3. 会員管理
- **会員一覧表示**: 全会員の情報を確認
- **会員無効化**: 会員を無効化（論理削除）
- **会員復元**: 無効化した会員を再度有効化
- ⚠️ 管理者アカウントは無効化できません

### 4. 商品管理
- **商品一覧表示**: 全商品の情報を確認
- **商品追加**: 新しい商品を登録
  - 基本情報（商品名、説明、価格、在庫など）
  - アレルゲン情報
  - 商品特徴
  - 販売設定（有効/無効）
- **商品編集**: 既存商品の情報を更新
- **商品無効化**: 商品を販売停止（論理削除）
- **商品有効化**: 無効化した商品を再度販売

### 5. 注文管理
- **注文一覧表示**: 全注文の詳細を確認
- 注文番号、会員情報、金額、ステータスなどを表示
- 注文のステータス別に集計

## データベース構造の変更点

### 新規追加カラム
```sql
-- membersテーブル
ALTER TABLE members 
ADD COLUMN role VARCHAR(20) NOT NULL DEFAULT 'USER';

-- roleの値:
-- 'USER': 一般会員
-- 'ADMIN': 管理者
```

### 推奨する今後の拡張機能

1. **注文ステータス管理**
   - 注文のステータスを変更できる機能
   - ステータス変更履歴の記録

2. **お問い合わせ管理**
   - お問い合わせ一覧表示
   - 返信機能
   - ステータス管理

3. **売上レポート**
   - 期間別売上グラフ
   - 商品別売上ランキング
   - カテゴリー別分析

4. **在庫管理**
   - 在庫アラート機能
   - 自動在庫補充通知
   - 在庫履歴管理

5. **クーポン管理**
   - クーポンの作成・編集・削除
   - 利用状況の確認

6. **複数管理者対応**
   - 管理者の権限レベル設定
   - 操作ログの記録

## API エンドポイント

### 管理者API（新規）
| メソッド | パス | 説明 |
|---------|------|------|
| GET | /admin | 管理者ダッシュボード |
| GET | /admin/members | 会員一覧 |
| POST | /admin/members/{id}/delete | 会員無効化 |
| POST | /admin/members/{id}/restore | 会員復元 |
| GET | /admin/products | 商品一覧 |
| GET | /admin/products/new | 商品追加フォーム |
| POST | /admin/products/new | 商品追加処理 |
| GET | /admin/products/{id}/edit | 商品編集フォーム |
| POST | /admin/products/{id}/edit | 商品更新処理 |
| POST | /admin/products/{id}/delete | 商品無効化 |
| POST | /admin/products/{id}/activate | 商品有効化 |
| GET | /admin/orders | 注文一覧 |

### 既存API
| メソッド | パス | 説明 |
|---------|------|------|
| GET | / | トップページ |
| GET | /products/{id} | 商品詳細 |
| GET | /search?keyword= | 商品検索 |
| GET | /login | ログインページ |
| POST | /login | ログイン処理 |
| GET | /register | 会員登録ページ |
| POST | /register | 会員登録処理 |
| GET | /member | マイページ |
| GET | /cart | カート表示 |
| POST | /cart/add | カートに追加 |
| POST | /cart/update/{id} | 数量更更新 |
| POST | /cart/remove/{id} | カートから削除 |
| GET | /checkout | チェックアウト |
| POST | /checkout/confirm | 注文確認 |
| POST | /checkout/complete | 注文確定 |

## 開発者情報

このプロジェクトはポートフォリオ用に作成されました。

## ライセンス

MIT License

## トラブルシューティング

### 管理者ページにアクセスできない場合

1. **管理者アカウントでログインしているか確認**
   - 管理者権限を持つアカウントでログインする必要があります
   - テスト管理者アカウント: `admin@healsweets.com` / `admin123`

2. **データベースにroleカラムが存在するか確認**
   ```sql
   DESC members;
   ```
   roleカラムがない場合は `sql/add_admin_role.sql` を実行

3. **会員のroleが正しく設定されているか確認**
   ```sql
   SELECT id, email, role FROM members;
   ```

### 商品画像が表示されない場合

1. **画像ファイルの配置を確認**
   - `src/main/resources/static/images/` に画像を配置
   - 商品のimageUrlと一致するか確認

2. **画像URLの形式を確認**
   - 正しい形式: `/images/item01.jpg`
   - 間違った形式: `images/item01.jpg` または `C:/images/item01.jpg`

### 会員登録がDBに反映されない場合

1. **MySQLの接続確認**
   ```bash
   mysql -u root -p -e "USE healsweets; SHOW TABLES;"
   ```

2. **テーブルが存在するか確認**
   - テーブルがない場合は `sql/init_database.sql` を実行

3. **アプリケーションログの確認**
   ```bash
   mvn spring-boot:run -Dspring-boot.run.profiles=mysql 2>&1 | grep -i error
   ```

## 画像ファイルについて

`src/main/resources/static/images/` フォルダに以下の画像を配置してください：

### 商品画像
- item01.jpg ～ item10.jpg

### ヒーロー画像（スライドショー用）
- CS_cake_1_pc.jpg
- CS_cake_2_pc.jpg
- CS_cake_3_pc.jpg

詳細は `src/main/resources/static/images/README.md` を参照してください。
